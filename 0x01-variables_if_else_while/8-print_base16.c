#include <stdio.h>


/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char lowcase;
	int i;

	for (i = '0'; i <= '9'; i++)
        {
                putchari);
        }
	for (lowcase = 'a'; lowcase <= 'f'; lowcase++)
	{
		putchar(lowcase);
	}
	putchar('\n');
	return (0);
}
